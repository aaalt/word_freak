//< get set all adt and files


//< allocate memory for hash and trie, open files
//< returns NIL if error
FILE* set_start(FILE* f);

//< free memory form hash and trie, close files
UJ set_end(FILE* f);